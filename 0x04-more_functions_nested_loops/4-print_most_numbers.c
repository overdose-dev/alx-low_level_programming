#include "main.h"

/**
 * print_most_numbers - function that prints numbers
 *
 * Return: always 0
 */

void print_most_numbers(void)
{
	int i;

	if (i != 2 && i != 4)
	{
	for (i = 0 ; i <= 10 ; i++)
		_putchar(i + '0');
	}
	_putchar('\n');
}
